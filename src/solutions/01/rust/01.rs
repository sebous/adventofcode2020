// use crate::lib::input;
use std::fs;
use std::path::PathBuf;

pub fn day01() {
    let a = PathBuf::from("./solutions/01/data.txt");
    println!("{:?}", a);
    // input::load_input("../data.txt");
}